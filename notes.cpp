#include <iostream>

using namespace std;

int main(){
    cout << "lol";

    return 0;
}

/*
WEEK 12: ABSTRACT DATA TYPES WHAAAAAAAAAAAAAAAAAAAAAAAT

> a data type whose representation is hidden
    > focus on operations, ignore the data representation


> C++ ADTs
    > string complex valarray
        > collections:
        - array
        - vector
        - list
        - stack
        - queue
        - set
        - map

        > others:
        - date
        - counter
        - stopwatch

> member functions are the API of an ADT
    > encapsulate data types within implementation
        > avoid clients relying on implementation details
        > allow change in implementation


> templates
    > e.g., vectors (std::vector<int> name) is a template class
    > 
*/